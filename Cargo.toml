[package]
description = "ternary-kernel-launch - SuperInstance ecosystem crate"
repository = "https://github.com/SuperInstance/ternary-kernel-launch"
name = "ternary-kernel-launch"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
